# Drupal (Decoupled.io) - Created by setup script
NEXT_PUBLIC_DRUPAL_BASE_URL=https://your-space.decoupled.website
DRUPAL_CLIENT_ID=your-client-id
DRUPAL_CLIENT_SECRET=your-client-secret
DRUPAL_REVALIDATE_SECRET=your-revalidate-secret

# Shopify Storefront API
# Get these from Shopify Admin > Settings > Apps > Develop apps > Create app
# Then: Configure Storefront API scopes > Install app > API credentials
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN=your-storefront-access-token

# Shopify Admin API (optional - for seeding sample products)
# Only needed if you want the setup script to create sample products
SHOPIFY_ADMIN_ACCESS_TOKEN=your-admin-access-token
